// Theme utilities for the CopperX bot
// This file acts as a compatibility layer for existing code
// Import all theme-related constants from the central constants folder

import {
  COLORS,
  SECTION,
  DIVIDERS,
  ICON,
  formatHeader,
  formatSubheader,
  formatSuccess,
  formatError,
  formatWarning,
  formatInfo,
  formatLoading,
  formatAmount,
  formatNetworkIcon,
} from '../constants';

// Re-export everything to maintain backward compatibility
export {
  COLORS,
  SECTION,
  DIVIDERS,
  ICON,
  formatHeader,
  formatSubheader,
  formatSuccess,
  formatError,
  formatWarning,
  formatInfo,
  formatLoading,
  formatAmount,
  formatNetworkIcon,
}; 